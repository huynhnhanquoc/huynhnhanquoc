# Local reference only — the Vietnamese source template for the about page.
# It is a site view fragment ({{ ... }} bindings, live /storage/ paths), not a
# document that renders on GitHub. The published English version is about/README.md.
about/readme.html

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
